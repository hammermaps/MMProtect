# src Placeholder

Die eigentlichen Implementierungsprojekte sollen durch Coding-Agenten erzeugt werden:

```text
src/LicenseServer
src/EncoderCli
src/PhpDecoderLoader
```

Dieses Paket enthält bewusst Spezifikation, Skripte, Tests und Konfigurationen, aber keine vollständige Implementierung.
